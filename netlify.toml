[build]
  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build.
  publish = "."

  # Command to run before publishing. We have no build step, so we leave it empty.
  command = ""
